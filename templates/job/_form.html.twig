
{{ form_start(form, {attr: {'novalidate': 'novalidate'} }) }}

    {{ form_widget(form) }}

    <button class="btn btn-transparent">{{ button_label|default('Save'|trans) }}</button>

{{ form_end(form) }}
